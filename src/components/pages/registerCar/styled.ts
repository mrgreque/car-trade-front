import { Card } from "@mui/material";
import styled from "styled-components";

export const StyledCardPreview = styled(Card)`
  width: 100%;
  height: 450px;
  display: flex;
  justify-content: center;
  align-items: center;
`;

export const StyledArticlePreview = styled.article`
  max-width: 300px;
  width: 100%;
`

export const StyledTtlePreview = styled.p`
  font-size: 1.5em;
  text-align: center;
  margin: 10px 0;
  padding: 0;
`
